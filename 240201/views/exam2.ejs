<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/postExam" method="post">
        이름 <input type="text" name="name" required />
        <fieldset>
            <legend>성별</legend>
            <input type="radio" name="genders" id="man" value="man" required />
            <label for="man">남자</label>
            <input type="radio" name="genders" id="woman" value="woman" required />
            <label for="woman">여자</label>
        </fieldset>

        <fieldset>
            <legend>생년월일</legend>
            <select name="year" id="year">
                <optgroup label="년">
                    <% for (let i = 1970; i <= 2024; i++) { %>
                    <option value="<%= i %>"><%= i %></option>
                    <% } %>
                </optgroup></select
            >년

            <select name="month" id="month">
                <optgroup label="월">
                    <% for (let i = 1; i <= 12; i++) { %>
                    <option value="<%= i %>"><%= i %></option>
                    <% } %>
                </optgroup></select
            >월

            <select name="day" id="day">
                <optgroup label="일">
                    <% for (let i = 1; i <= 31; i++) { %>
                    <option value="<%= i %>"><%= i %></option>
                    <% } %>
                </optgroup></select
            >일
        </fieldset>

        <fieldset>
            <legend>관심사</legend>
            <input type="checkbox" name="inters" id="travel" value="travel" />
            <label for="travel">여행</label>
            <input type="checkbox" name="inters" id="fashion" value="fashion" />
            <label for="fashion">패션</label>
            <input type="checkbox" name="inters" id="food" value="food" />
            <label for="food">음식</label>
        </fieldset>

        <fieldset>
            <legend>선택</legend>
            <label for="favoriteColor">좋아하는 색상</label>
            <input type="color" name="favoriteColor" id="favoriteColor" value="favoritColor" />
            <br />
            <label for="favoriteNum">좋아하는 숫자</label>
            <input type="number" name="favoriteNum" id="favoriteNum" value="favoriteNum" />
        </fieldset>
        <br />
        <button type="submit">회원가입</button>
</body>
</html>